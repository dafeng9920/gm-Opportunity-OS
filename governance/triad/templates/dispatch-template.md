# Dispatch template

Required fields: task id, admission objective, input references, expected output, and `scope: admission`.

The generated plan assigns exactly Execution, Review, and Compliance. It has no tool invocation, runtime, adapter, queue-consumer, or Core write instruction.
