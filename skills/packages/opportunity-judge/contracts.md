# Opportunity Judge Skill contracts

Input: `JudgeInput v0.1` (existing `opportunity.judge.contracts.JudgeInput`).

Output: `JudgeAssessment v0.1` (existing `opportunity.judge.contracts.JudgeAssessment`).

This package defines invocation and validation only. It does not implement, invoke, or configure an Agent.
